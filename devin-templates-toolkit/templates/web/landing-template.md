# Landing Page Template

## Structure

- `src/routes/index.tsx` — hero, features, CTA
- `public/` — images, favicon
- `index.html` — meta tags, Open Graph

## Tech Stack

- SolidJS
- Vite
- UnoCSS
- TanStack Router

## Setup

1. Copy to project
2. Replace `<project-name>` and `<repo-name>`
3. รัน `bun install && bun run build`
