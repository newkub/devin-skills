{
  "name": "devin-global-skills-cli",
  "version": "1.0.0",
  "description": "CLI สำหรับจัดการ devin global skills",
  "type": "module",
  "bin": {
    "devin-global-skills-cli": "dist/cli.js"
  },
  "scripts": {
    "dev": "bun run src/presentation/cli.ts",
    "build": "bun build src/presentation/cli.ts --outdir dist --target bun --minify",
    "test": "bun test"
  },
  "dependencies": {
    "citty": "^0.1.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
