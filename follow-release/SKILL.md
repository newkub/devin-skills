---
name: follow-release
description: ??????????????? release strategy ???????? npm, crates, Docker, VSCode ??? monorepo versioning
---

## Goal

??????????????? release strategy ????????????? project ???????? npm, crates.io, Docker, VS Code Marketplace, preview ??? monorepo versioning ????? setup ??? deploy ????? platform

## Scope

???????? project ???? release ????? external platforms ?????????????? versioning, changelog ??? preview releases ????????:
- npm — Auto ???? semantic-release ?????? automated npm publish
- crates.io — release-plz ???? cargo-release ?????? Rust crates
- Docker — Docker Hub ???? GHCR ?????? container images
- VS Code Marketplace — VSCE ?????? extension publish
- Preview — pkg.pr.new ???? Changesets Snapshot
- Monorepo — Changesets ?????? versioning ??? changelogs

## Execute

### 1. Detect Release Targets

> Goal: ???? platforms ??? project ?? release

1. ???? `package.json` ??????? `private`, `publisher`, `workspaces`
2. ???? `Cargo.toml` ????? Rust crate
3. ???? `Dockerfile` ????? container image
4. ???? `package.json` ??????? `publisher` ??? `engines.vscode` ?????? VS Code extension
5. ??? user ????????????????????? release ??????

### 2. Choose Release Strategy

> Goal: ????? strategy ??????????

?????????? release ????? platform ????:
- npm/bun: ?? Step 3 (npm) ???? `/follow-tool-auto-it` ?????? conventional commits
- Rust: ?? Step 4 (crates)
- Docker: ?? Step 5 (Docker)
- VS Code: ?? Step 6 (VS Code)
- semantic-release: ?? `/follow-tool-semantic-release`

?????????? preview ??? PR/commit:
- ?? `/follow-tool-pkg-new` ?????? continuous preview releases

?????????? versioning/changelog ?? monorepo:
- ?? `/follow-tool-changesets` ?????? manage versioning ??? changelogs

### 3. Release To npm

> Goal: ??????? Auto ?????? automated releases ????? npm

1. ??????? `package.json` ?? `name`, `description`, `version`, `author` ???
2. ??????? `private: false` ??? `publishConfig.access: public`
3. ??? `bun add -D auto` ???????? config ?? `package.json`
4. ????? script `release: auto shipit` ?????????? `baseBranch` ???? `main`
5. ????? NPM token (Automation token) ???????? `NPM_TOKEN` ?? GitHub secrets ???? `gh secret set NPM_TOKEN`
6. ????? `.github/workflows/release.yml` ????? `permissions: contents write, pull-requests write` ??? trigger ?? push ?? main

### 4. Release To crates.io

> Goal: ??????? Cargo ?????? automated releases ????? crates.io

1. ??????? `Cargo.toml` ?? `name`, `version`, `authors`, `description`, `license`, `repository`, `homepage`, `keywords`, `categories`
2. ??? `cargo install release-plz` ???? `cargo install cargo-release` ??????? release-plz action
3. ????? config ?? `release-plz.toml` ???? `.release.toml` ????? `changelog_config` conventional ??? `release_branch: main`
4. ????? API token ??? crates.io ???????? `CARGO_REGISTRY_TOKEN` ?? GitHub secrets
5. ????? `.github/workflows/release.yml` ????? `permissions: contents write, pull-requests write`, `fetch-depth: 0`, `persist-credentials: false` ??? `release-plz-release` + `release-plz-pr` jobs

### 5. Release To Docker

> Goal: ??????? Docker images ?????? automated releases ????? Docker Hub ???? GHCR

1. ??????? `Dockerfile` ?? base image, labels, multi-stage build ??? `.dockerignore`
2. ????? repository ?? Docker Hub ???? GitHub Container Registry ?????????? image name ??? tags
3. ????? access token ??? Docker Hub ??????? `GITHUB_TOKEN` ?????? GHCR
4. ????? `DOCKER_USERNAME`, `DOCKER_PASSWORD` ?? environment variables
5. ????? `.github/workflows/release.yml` ????? `permissions: contents write, packages write` ??? trigger ?? push ?? main

### 6. Release To VS Code Marketplace

> Goal: ??????? VS Code extensions ?????? automated releases ????? Marketplace

1. ??????? `package.json` ?? `publisher`, `name`, `version`, `displayName`, `description`, `repository`, `bugs`, `homepage`, `engines.vscode`, `categories`, `keywords`
2. ??? `bun add -D @vscode/vsce` ???? `bun add -g @vscode/vsce`
3. ????? publisher ?? `marketplace.visualstudio.com` ?????????? `package.json`
4. ????? PAT token ??? Azure DevOps ???????? `VSCE_PAT` ?? environment variables ??????? `vsce login publisher-name`
5. ????? `.github/workflows/release.yml` ????? `permissions: contents write`, `VSCE_PAT` env ??? trigger ?? push ?? main

### 7. Combine Changesets And pkg.pr.new

> Goal: ???????????????????????????

- ??? `pkg.pr.new` ?????? preview ??? PR/Commit ??????????? npm
- ??? `Changesets` ????? merge ???? main ????? release ?????? npm
- ?????????? snapshot release ???? Changesets ????? script:
  ```json
  {
    "scripts": {
      "release:snapshot": "changeset version --snapshot canary && changeset publish --tag canary"
    }
  }
  ```

### 8. Compare Preview Options

> Goal: ????? preview release tool ??????????

| ????????? | `Changesets Snapshot` | `pkg.pr.new` |
| --- | --- | --- |
| ?????????????? | Publish ?? `npm Registry` ???? | ???????? `CDN` ??? StackBlitz |
| ??????????? | `bun add package-name@0.0.0-snapshot-...` | `bun add https://pkg.pr.new/owner/repo` |
| ????????? | ????? `version history` ?? npm | ???????? `npm Registry` |
| ?????????? | ?????? `NPM_TOKEN` ??? Snapshot script | Zero Config ?????????? Token |

### 9. Run Release

> Goal: Release ?????????

1. ??????? tokens/permissions ???? release (`NPM_TOKEN`, `CARGO_REGISTRY_TOKEN`, `VSCE_PAT`, `DOCKER_PASSWORD`)
2. ?? `/run-release` ?????? multi-platform release ?????????
3. ??????? tags, changelogs ??? published artifacts

## Rules

### 1. Strategy Selection

- ??? `pkg.pr.new` ?????? preview ??? Commit/PR
- ??? `Changesets` ?????? release ??????? changelog
- ??? `semantic-release` ???? `auto` ?????? conventional commit based release

### 2. Package Configuration

- npm: ?????? `name`, `description`, `version`, `author`, `private: false`, `publishConfig.access: public`
- crates: ?????? `name`, `version`, `authors`, `description`, `license`, `repository`, `homepage`, `keywords`, `categories`
- Docker: ?????? `Dockerfile` valid, base image, labels, multi-stage build, `.dockerignore`
- VS Code: ?????? `publisher`, `name`, `version`, `displayName`, `description`, `engines.vscode`, `repository`, `categories`, `keywords`

### 3. Authentication

- npm: ?????? `NPM_TOKEN` ?? GitHub secrets ??? `GITHUB_TOKEN`
- crates: ?????? `CARGO_REGISTRY_TOKEN` ?? secrets ??????? trusted publishing
- Docker: ?????? `DOCKER_USERNAME`, `DOCKER_PASSWORD` ???? `GITHUB_TOKEN` ?????? GHCR
- VS Code: ?????? `VSCE_PAT` ??? Azure DevOps ???? `vsce login`

### 4. GitHub Workflow

- ?????? `permissions` ????????????????????? platform
- ??????? trigger ?? push ?? main branch
- crates: ?????? `fetch-depth: 0`, `persist-credentials: false`, `concurrency` ?????? release-pr job

### 5. Safety

- ??? workflow preview ??? release ????
- ??? publish preview ?? npm ????????????????
- ??????? tokens ???????
- ??? `Changesets Snapshot` ????????? `version history` ?????
- `pkg.pr.new` ???????? PR preview ???????? npm Registry

## Expected Outcome

- Release strategy ?????????? project
- npm, crates.io, Docker, VS Code Marketplace setup ???????
- Tools ????????????????????????
- Authentication tokens ???????????
- GitHub workflows ????????????????????????? platform
- Preview ??? release ???????????? flow
- ????? conflict ??????? snapshot ??? official versions
