# Templates Index

## Prefix To Template Mapping

- `follow-*-architecture` → architecture template
- `follow-*` → follow template
- `run-*` → run template
- `check-*` → check template
- `review-*` → review template
- `update-*` → update template
- `gen-*` → gen template
- `report-*` → report template
- `idea-*` → idea template
- `lib-*` → lib template
