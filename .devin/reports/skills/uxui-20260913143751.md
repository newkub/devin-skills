# UX/UI Improve Report — open-devin-in-web

- **Date**: 2026-09-13 14:37
- **App**: `open-devin-in-web` (SolidJS + vis-network graph, route `/` only)
- **Server**: vite `localhost:5173` + oRPC `localhost:3000` (live data)
- **Graph at test time**: 669–671 nodes, ~3,611–3,616 edges (live scan; counts shift as skills repo changes)
- **Method**: `agent-browser` roleplay + screenshots (desktop 1280×720, mobile 390×844), page `eval` inspection, direct API calls

## Findings

| No. | route | dimension | finding | severity | fix | status |
|-----|-------|-----------|---------|----------|-----|--------|
| 1 | / | functional | **All graph effects dead** — `createEffect` guards read non-reactive `let network`; short-circuit meant `colored()`/`raw()` never tracked → filter/search/cluster/issues/focus/zoom/theme never updated canvas (DOM counters changed, canvas did not) | Critical | added reactive `netReady` signal; all effects gate on `netReady()` | fixed — cluster 670→10 nodes, issues→19, ego→259 verified |
| 2 | / | functional | Review data silently missing — `REVIEW_REPORT` default pointed at renamed dir (`review-devin-global-skills` → `review-devin-harness` → `review-devin-global-harness`); `review: null`, issues filter showed 0 | Critical | default path updated + `resolveReviewPath()` glob fallback `review-devin*/review-skills-report.json` | fixed — grade/issues load consistently across requests |
| 3 | / | visual/a11y | Light mode broken — `body { color: var(--text) }` resolves at `:root`; `.app.light` var override never reached inheriting elements → light-on-light text (sidebar lists unreadable), dark canvas bg | High | `.app { background: var(--bg); color: var(--text) }` | fixed — `rgb(30,41,59)` text verified, canvas light |
| 4 | / | responsive | Mobile topbar (390px) overflowed horizontally — selects clipped to `¿` glyphs (46px), controls unreachable | High | `@media ≤720px`: topbar `flex-wrap`, search full-width row, selects `min-width:110px`, `.brand` hidden, `.detail-float` clamped | fixed — all controls reachable, selects readable |
| 5 | / | a11y | Icon-only `.tb-btn` buttons had `title` but no `aria-label` | Medium | `aria-label` added to all 12 icon buttons | fixed |
| 6 | / | a11y | Clickable `<li>` (top skills, health, search results, detail uses/used-by) not keyboard-focusable | Medium | `role="button" tabIndex={0}` + Enter handler; `[role="button"]:focus-visible` outline | fixed |
| 7 | / | uxui-feature | Silent actions — `copy`, `copy /`, PNG export gave no feedback | Medium (UXUI feature) | transient `.toast` (`role="status"`, 2s) — "copied id", "copied /", "exported PNG" | added — verified "copied id" |
| 8 | / | visual | Search placeholder truncated "search skills (p…" on desktop | Low | — | open (cosmetic; input has title tooltip) |
| 9 | / | infra | Stale bun servers on `:3000` served old payloads across restarts (port held by orphan PIDs) | Medium | killed orphans, restarted clean; verified single listener | fixed |

## Functional coverage (agent-browser, exploratory)

| No. | flow | result |
|-----|------|--------|
| 1 | click top-skill list item → detail card | PASS — heading, in/out counts, USES list |
| 2 | search "deep-plan" → dropdown → select | PASS — "2 matches", selects + detail card |
| 3 | sidebar tabs explore/stats/help + collapsible sections | PASS |
| 4 | issues-only toggle (H) | FAIL→PASS — was dead effect + stale server; now filters to 19 nodes |
| 5 | cluster mode (C) | FAIL→PASS — was dead effect; now renders 10 prefix hexagons |
| 6 | ego net | PASS — 669→259 neighbor view |
| 7 | theme toggle (D) | FAIL→PASS — light mode was unreadable; now flips correctly |
| 8 | copy / export feedback | enhanced — toast added |
| 9 | console/page errors during pass | 0 app errors (external CAPTCHA-tab noise ignored) |

## Verification

- `bunx tsc --noEmit` — exit 0
- `bun run build` — exit 0
- API `skillsGraph` — consistent across repeated calls (nodes 670, review meta present)
- Re-screenshots confirm desktop dark, desktop light, mobile wrapped topbar

## Round 2 (post-perf pass, 2026-09-13 ~16:00)

| No. | route | dimension | finding | severity | fix | status |
|-----|-------|-----------|---------|----------|-----|--------|
| 10 | / | functional | Search dropdown stayed open after Enter/click select (overlaid canvas + detail) | Low | gate on `searchFocused`, `li` uses `onMouseDown` (fires before blur), blur input after select | fixed — verified |
| 11 | infra | reliability | oRPC server + vite died mid-pass; stale vite (pid from 13:23) held `::` :5173 while new vite bound `::1` → 404s | infra | killed stale vite pids, restarted both | fixed |

Round-2 verified flows: `/`→type→Enter keyboard select, prefix filter (182/669), type∩prefix (0), Esc clear, random node, issues toggle (7 visible), cluster (107 groups) + restore, hover-dim, zoom label fade, mobile light + detail card, dark focused view. Errors: 0.

## Round 3 (2026-09-13 ~16:30)

| No. | route | dimension | finding | severity | fix | status |
|-----|-------|-----------|---------|----------|-----|--------|
| 12 | / | a11y/uxui-feature | Search results keyboard-only reachable via Enter=first match; no arrow-key navigation (non-first results needed mouse) | Medium | `searchIdx` signal, ArrowUp/Down cycle, `.sr-active` highlight + scrollIntoView, Enter picks active | added — verified (2×Down → review-dependencies selected) |
| 13 | infra | reliability | bun server died repeatedly when stdout piped through `Select-Object` (EPIPE); stale vite pids keep holding :5173 variants | infra | run server detached via `Start-Process` with log redirect | fixed |

Round-3 verified: stats/help tabs (review grade back to A/100 after repo cleanup), arrow-nav search, dropdown close, dark focused node view, mobile light. Errors: 0. tsc=0, build=0.

## Round 3 addendum — E2E suite

Playwright suite added: `tests/app.spec.ts` (7 tests, all passing ~2.2m) + `playwright.config.ts` (webServer auto-starts `bun server.ts` + vite, `reuseExistingServer`) + `/health` endpoint on the oRPC server for readiness. Covers: graph load, search+arrow-nav+enter select, prefix filter, issues toggle, cluster toggle+restore, theme toggle, Esc clear. Run: `bun run test:e2e`.

## Gaps / next actions

- ~~No Playwright suite~~ — resolved in round 3: `bun run test:e2e` (7 tests green).
- Review findings in live data are real (`review-devin-global-harness` score 46/F) — they reflect the in-flight skills-repo rename state, not app bugs.
- `window.__net` debug handle left in `Graph.tsx` — keep or remove before ship.
