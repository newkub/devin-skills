# Prepare Context

## Goal

เข้าใจ CLI และ spec target

## Checks

1. ตรวจว่า `usage.kdl` มีอยู่ ถ้าไม่ → flag เป็น critical
2. ระบุ CLI entry point เช่น `src/presentation/cli.ts` หรือ equivalent
3. อ่าน `package.json` เพื่อดู `name`, `version`, `bin`
