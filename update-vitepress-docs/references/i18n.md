---
title: I18n Reference
description: Multi-language docs setup for update-vitepress-docs (VitePress locales)
---

# I18n Reference

VitePress locales when docs need multiple languages (e.g. Thai + English).

## Structure

```
docs/
  index.md            # default locale (root)
  en/
    index.md          # /en/ locale
    ...
  .vitepress/config.ts
```

## Config

```ts
export default defineConfig({
  locales: {
    root: { label: 'ไทย', lang: 'th' },
    en: {
      label: 'English',
      lang: 'en',
      link: '/en/',
      themeConfig: {
        // nav/sidebar translated separately
      },
    },
  },
})
```

## Rules

- Default locale lives at `docs/` root — extra languages in `docs/<lang>/`
- Mirror the same page structure per locale — no divergent trees
- Do not mix languages inside one page (skill rule 5)
- Only add locales when the project actually ships multi-language content
